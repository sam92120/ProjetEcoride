
{% extends 'base.html.twig' %}

{% block title %}Ajouter une Voiture{% endblock %}

{% block body %}
<div class="container mt-4 mb-4 text-center p-4 rounded">
    <h1  class=" mt-4 mb-4">Ajouter une nouvelle voiture</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}

{% endblock %}